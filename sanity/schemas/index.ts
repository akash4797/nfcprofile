import user from "./user-schema";
import account from "./account-schema";
import verificationToken from "./verification-token-schema";

const schemas = [user, account, verificationToken];

export default schemas;
